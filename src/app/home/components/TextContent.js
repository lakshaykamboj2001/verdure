import React from 'react'
import styles from './Home.module.css'

const TextContent = () => {
  return (
    <div className={`${styles.text_content}`}>
        The circle that revive the art piece, reuse its soul and reimagine the designs.  Each silhouette is a quiet choice to restore legacy, not to replace.
    </div>
  )
}

export default TextContent